const webpack = require('webpack');

module.exports = {
    devServer: {
        allowedHosts: ['https://6rf89w-4200.csb.app'] // <  Copy and paste the domain from the URL Codebox provides for your runtime here
     },
};